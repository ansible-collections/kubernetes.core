---
- name: Main helm tests
  block:
    - name: Install helm-diff plugin
      helm_plugin:
        binary_path: "{{ helm_binary }}"
        plugin_path: https://github.com/databus23/helm-diff
        plugin_version: "{{ helm_version | string is ansible.builtin.version('4.0.0', '>=') | ternary('v3.14.0', 'v3.10.0') }}"
        verify: false

    - name: "Ensure we honor the environment variables"
      ansible.builtin.include_tasks: test_read_envvars.yml

    - name: Deploy charts
      ansible.builtin.include_tasks: "tests_chart/{{ test_chart_type }}.yml"
      loop_control:
        loop_var: test_chart_type
      with_items:
        - from_local_path
        - from_repository
        - from_url

    - name: Test helm upgrade with reuse_values
      ansible.builtin.include_tasks: test_helm_reuse_values.yml

    - name: Test helm upgrade with reset_then_reuse_values
      ansible.builtin.include_tasks: test_helm_reset_then_reuse_values.yml
      when: helm_version | string is ansible.builtin.version('3.14.0')

    - name: Test helm dependency update
      ansible.builtin.include_tasks: test_up_dep.yml

    - name: Test helm install with chart name containing space
      ansible.builtin.include_tasks: test_helm_with_space_into_chart_name.yml

    # https://github.com/ansible-collections/community.kubernetes/issues/296
    - name: Test Skip CRDS feature in helm chart install
      ansible.builtin.include_tasks: test_crds.yml

    - name: Test insecure registry flag feature
      ansible.builtin.include_tasks: test_helm_insecure.yml

    - name: Test take ownership flag feature
      ansible.builtin.include_tasks: test_helm_take_ownership.yml

    - name: Test helm skip_schema_validation
      ansible.builtin.include_tasks: test_skip_schema_validation.yml

    - name: Test helm wait_for_jobs
      ansible.builtin.include_tasks: test_helm_wait_for_jobs.yml

  always:
    - name: Remove helm-diff plugin
      helm_plugin:
        binary_path: "{{ helm_binary }}"
        plugin_name: diff
        state: absent
      ignore_errors: true

    - name: Clean helm install
      ansible.builtin.file:
        path: "/tmp/helm/"
        state: absent
